describe("Calculator tests", () => {
    test('adding 1 + 2 should return 10', () => {
      // arrange and act
      var result = 3
    
      // assert
      expect(result).toBe(3);
    });
    test('adding 2 + 2 should return 10', () => {
        // arrange and act
        var result = 4
      
        // assert
        expect(result).toBe(4);
      });
})
